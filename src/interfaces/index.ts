export interface IMeetingRecord {
    id: string
    topic: string
    description: string
    startdate: string
    starttime: string
    durationhrs: number
    durationmins: number
    user: string
}